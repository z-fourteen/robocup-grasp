# 输出目录

- `reconstruction/`：TSDF 重建和坐标系设置的运行结果。
- `inference/`：在线/离线位姿估计及抓取组合结果。
- `acceptance/`：尺寸、位姿重复性和集成验收报告。

输出应按 `任务/对象/运行时间或版本` 分层，例如
`reconstruction/cup_01/2026-08-27_run01/`。这里的内容应可由输入、配置和代码重新生成，因此默认
不纳入 Git。
