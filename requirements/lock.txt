# Reproducible P0-A baseline (validated with Python 3.10.12).
# Keep this file limited to the direct build, runtime, and test dependencies
# so that reports and CI can be compared across machines.
setuptools==84.0.0
numpy==1.26.4
Pillow==12.3.0
PyYAML==6.0.3
jsonschema==4.26.0
open3d==0.19.0
pytest==9.1.1
